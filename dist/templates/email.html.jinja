<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th,
        td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #8EAADB;
            color: white;
            font-size: 1.2em;
        }

        .label {
            font-weight: bold;
        }

        .header-content {
            display: flex;
            align-items: center;
            justify-content: flex-start;
        }

        .header-content > img {
            margin-right: 10px;
            width: 50px;
        }
    </style>
</head>

<body>
<div class="container">
    <table>
        <tr>
            <th colspan="2">
                <div class="header-content">
                    {{ maintenance_icon }}
                    <span>Network Services – Planned Maintenance</span>
                </div>
            </th>
        </tr>
        <tr>
            <td class="label">Summary description:</td>
            <td>{{ summary.replace('\n', '<br>')|safe }}</td>
        </tr>
        <tr>
            <td class="label">Status:</td>
            <td>{{ status }}</td>
        </tr>
        <tr>
            <td class="label">Who is impacted?</td>
            <td>{{ who_is_impacted }}</td>
        </tr>
        <tr>
            <td class="label">When?</td>
            <td>{{ when }}</td>
        </tr>
    </table>
</div>
</body>

</html>
